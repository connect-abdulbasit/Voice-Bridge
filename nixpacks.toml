[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = [
  "npm install",
  "cd backend(node) && npm install"
]

[phases.build]
cmds = [
  "cd backend(node) && npm run build"
]

[start]
cmd = "cd backend(node) && npm start"
